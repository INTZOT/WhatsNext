# 数据库连接 (Docker Compose 默认)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/whatsnext"

# NextAuth 密钥 (生产环境请用 `openssl rand -base64 32` 生成)
AUTH_SECRET="change-me-to-a-random-secret-in-production"

# 站点地址
AUTH_URL="http://localhost:3000"
